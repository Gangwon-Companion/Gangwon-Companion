# EC2 private IPv4 address, for example 10.20.0.25.
DATA_PRIVATE_IP=10.20.0.0

# Generate once with:
# sudo docker run --rm confluentinc/cp-kafka:7.8.0 kafka-storage random-uuid
KAFKA_CLUSTER_ID=replace-with-generated-cluster-id
