spring:
  application:
    name: gangwon-companion
  profiles:
    default: local

jwt:
  secret: ${JWT_SECRET}

naver:
  map:
    client-id: ${NAVER_MAP_CLIENT_ID}
    client-secret: ${NAVER_MAP_CLIENT_SECRET}
