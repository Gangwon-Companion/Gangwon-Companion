spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB:gangwon}
    username: ${POSTGRES_USER:gangwon_user}
    password: ${POSTGRES_PASSWORD:qwer1234}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:gangwon_jwt_secret_key_2024_very_long_string}

personal-data:
  encryption-key: ${PERSONAL_DATA_ENCRYPTION_KEY:gangwon_companion_aes_key_32byte}

captcha:
  enabled: ${CAPTCHA_ENABLED:false}
  secret: ${CAPTCHA_SECRET:}
  min-score: ${CAPTCHA_MIN_SCORE:0.5}

external:
  tour-api:
    service-key: ${TOUR_API_KEY}
    base-url: https://apis.data.go.kr/B551011/KorService2
  tats-cnctr-rate:
    service-key: ${TATS_CNCTR_RATE_KEY:${TOUR_API_KEY:}}
    base-url: https://apis.data.go.kr/B551011/TatsCnctrRateService
